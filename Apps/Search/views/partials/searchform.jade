h1 Search my Locker
#searchForm
  form(action='#{searchPath}', method='post')
    select.typeSelector(name='type', id='searchtype')
        option(value='') Search all types
        option(value='contacts') Search only contacts
        option(value='photos') Search only photos
        option(value='status/twitter') Search only Twitter statuses
    input.text(type='text', id='searchterm', name='searchterm', size='48')
    #searchButton
      .button.clb
        a#searchButton(href='#') Search
  .clb