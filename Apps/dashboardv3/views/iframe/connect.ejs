<!DOCTYPE html>

<html>
  <head>
    <%- partial('header_partial') %>
    <link rel="stylesheet" href="css/connect.css" />
    <script src="js/locker.js"></script>
    <script src="js/connect.js"></script>
    <script type="text/javascript" charset="utf-8">
      $(function() { //$.cookie("firstvisit", true, {path: '/' }); });
    </script>
  </head>
  <body>
    <div id="main-header-1">
      <h1>Welcome! Connect services to begin.</h1>
      <h2>We don't post to these services, we just save the data for you.</h2>
      <div class="learnmore-link orange">Learn more about what we do</div>
      <div class="learnmore-copy">
        We save and normalize your data from these services so that, for
        instance, your photos from Twitter, Facebook, Instagram, Flickr, etc.
        are all saved in your individual account for you.  Once you've added
        data to your Singly account from these social media services, you'll
        be able to add apps to your account that remix your data in new, cool
        ways.
      </div>
    </div>
    <h1 id="main-header-2">
      Great! Connect a few more or
      <a target="_top" href="/dashboard/contactsviewer"
         class="orange" id="start-exploring-link">
        start exploring
      </a>.
    </h1>
    <ul class="synclets-list">
      <% for (var i = 0; i < connectors.length; i++) { %>
        <% if (!connectors[i].repository.hasOwnProperty('hidden') || connectors[i].repository.hidden !== 'true') { %>
        <li class="featured hideable">
          <div class="synclet-logoname-container">
            <img class="synclet-logo"
                 src="img/icons/32px/<%= connectors[i].name %>.png" alt="" />
            <span class="synclet-name">
              <%= connectors[i].repository.title %>
            </span>
          </div>
          <% if (connectors[i].hasOwnProperty('authed')) { %>
           <input class="action-button disabled" value="Connected" />
          <% } else { %>
          <input class="oauthLink action-button" value="Connect"
             data-provider="<%= connectors[i].repository.handle %>"
             data-width="<%= connectors[i].repository.oauthSize.width %>"
             data-height="<%= connectors[i].repository.oauthSize.height %>" />
           <% } %>
           <div class="gets-list">
            Gets: &nbsp;
            <% if (connectors[i].hasOwnProperty('provides')) {
                 for (var j=0; j<connectors[i].provides.length; j++) { %>
                   <%= connectors[i].provides[j] %>
                   <% if (j < connectors[i].provides.length - 1) { %>
                     ,
                   <% } %>
              <% }
            } %>
           </div>
        </li>
        <% } %>
      <% } %>
    </ul>
  </body>
</html>
